{
    "presets": [
        ["@babel/preset-env", { "modules": false }],
        ["@babel/preset-react", { "runtime": "classic" }]
    ],

    // Adds .js extension to the back
    "plugins": [["replace-import-extension", { "extMapping": { "": ".js" } }]]
}
